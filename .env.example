# Absolute origin used for canonical URLs, Open Graph and sitemap output.
NEXT_PUBLIC_SITE_URL=https://closeagain.com

# Early-access form delivery endpoint. See lib/early-access.ts.
# While this is unset the form intentionally reports that delivery is not
# connected instead of faking a successful submission.
NEXT_PUBLIC_EARLY_ACCESS_ENDPOINT=
